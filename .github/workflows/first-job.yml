name: first-job
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome message
        run: |
            echo "Welcome to first yaml code"
            echo "Trying multiple commands in a single run command"
      - name: print completion message
        run: echo "done with first workflow"
